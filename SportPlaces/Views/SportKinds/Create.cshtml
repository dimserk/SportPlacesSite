@model SportPlaces.Models.SportKind

@{
    ViewData["Title"] = "Create";
}

<h2>Добавление</h2>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="SportKindName" class="control-label">Вид спорта</label>
                <input asp-for="SportKindName" class="form-control" />
                <span asp-validation-for="SportKindName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Добавить" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Обратно к списку</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
